apiVersion: v1
kind: Secret
metadata:
  name: dockerconfigjson
  namespace: cinemaabyss
  annotations:
    cinemaabyss.io/note: "Template only. Use dockerconfigsecret.local.yaml for private GHCR images."
data:
  # Placeholder for {"auths":{"ghcr.io":{}}}.
  # Replace locally or use dockerconfigsecret.local.yaml for private GHCR images.
  # Do not commit a real GitHub PAT to the repository.
  .dockerconfigjson: eyJhdXRocyI6eyJnaGNyLmlvIjp7fX19
type: kubernetes.io/dockerconfigjson
